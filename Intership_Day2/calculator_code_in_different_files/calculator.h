#ifndef CALCULATOR_H // It works as a guard in c language...It prevent storage...if, 
#define CALCULATOR_H  // by mistake we included the header file more than 1 time 

int menu_list(void);

int addition(int, int);
int subtraction(int, int);
int multiplication(int, int);
int division(int, int);

#endif
