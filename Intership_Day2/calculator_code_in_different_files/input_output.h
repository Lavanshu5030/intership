#ifndef INPUT_OUTPUT_H // It works as a guard in c language...It prevent storage...if, 
#define INPUT_OUTPUT_H  // by mistake we included the header file more than 1 time 


void take_input(int *, int *);
void print_result(int);

#endif